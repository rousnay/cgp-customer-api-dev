export enum UserType {
  ADMIN = 'admin',
  CUSTOMER = 'customer',
  WAREHOUSE_OWNER = 'warehouse_owner',
  RIDER = 'rider',
  USER = 'user',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum AddressType {
  SHIPPING = 'shipping',
  BILLING = 'billing',
  PICKUP = 'pickup',
}
